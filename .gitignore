.DS_Store
.wrangler/
node_modules/

# Claude Code 的本機狀態（settings.json 進版控，供協作者共用設定）
.claude/projects/
.claude/settings.local.json

# ── 設計稿 ────────────────────────────────────────────────────────────
# 設計稿本體不進版控（2026-09-11 決定），**但 mockup/assets 是例外**
# （2026-09-14 改）：CI 要建得起前台就一定要有它。
#
# 🔴 例外只到 assets/ 為止。設計稿 HTML（640 KB）與 mockup2／mockup3 仍然擋著，
#    reference/ 的院方原始照片更不用說。放行 assets/ 的理由很具體：
#    那 12 MB 的每一個檔案本來就逐一公開掛在網站上，進公開 repo 不多洩漏任何東西，
#    最大單檔 708 KB 也低於 pre-push 的 2 MB 閘。
#
# ⚠️ 連帶後果要知情：`pnpm --filter web build` 第一步 `sync:assets` 會從
#    mockup/assets 複製樣式，**找不到就 exit 1**——所以它非進版控不可。
#    但 `pnpm verify:css` 另外要拿 **mockup 的 HTML** 當 class 詞彙基準，
#    那些沒進版控，所以**驗收閘只能在有設計稿的機器上跑，CI 跑不了**。
mockup/*
!mockup/assets/
mockup2/
mockup3/

# ── .NET ─────────────────────────────────────────────────────────────
# 建置產物。⚠️ 漏了這幾條，functions/bin 會塞進 6.5 MB 的 Roslyn DLL，
# 而 tools/sync-public.sh 的單檔 2 MB 斷言會擋下推送（2026-09-11 實際踩到）。
[Bb]in/
[Oo]bj/
functions/local.settings.json

# ── frontend ─────────────────────────────────────────────────────────
# 其餘規則在 frontend/.gitignore（.nuxt／.output／public/assets 等產物）

# 建置期資料快照（docs/09 §3）：由 tools/content-export 從資料庫產生，不進版控。
apps/web/content/

# 黃金樣本（tools/content-import/golden-diff.sh）
.golden-html/

# ── 舊站抓取的原始檔 ─────────────────────────────────────────────────
# tools/legacy-import 的抓取快取（舊站 HTML／WP JSON／原圖）。
# ⚠️ 這是「對正式站發過的請求」的存檔，重跑解析不必再敲對方主機 ——
#    不要因為它佔空間就刪掉，刪了就得整批重抓 1100 次。
tools/legacy-import/.cache/
# 解析產物（可由 .cache 重跑產生）。parsed-*.json 合計 10 MB，
# 超過 .githooks/pre-push 的單檔 2 MB 上限。
tools/legacy-import/discovered-*.json
tools/legacy-import/parsed-*.json
tools/legacy-import/images.json
