# 後台要打的 API 位址。
#
# 不設定時用正式站的 https://api.20skin.tw/api/v1（見 src/api/http.ts 的 DEFAULT_BASE）。
#
# ⚠️ 一定是**絕對網址**。後台在 20skin.tw/admin/、API 在 api.20skin.tw，是兩個網域
#    （docs/10 §2、docs/07 §1）；寫成相對路徑會打到 SWA 自己的 /api，那裡只有
#    fallback 一支（CLAUDE.md 決策 7）。
#
# ⚠️ 本機開發時 Functions 預設跑在 7071，但本專案的 host.json 有 routePrefix=api/v1，
#    而 content-import 的腳本預設用 7077 —— 以你實際啟動 func 時印出的 port 為準。
#
# ⚠️ 換這個值之後，**API 那頭的 CORS allow-list 也要放行你的來源**（http://localhost:3300）。
#    少了它瀏覽器只會給一個沒有任何資訊的 network error，看起來像斷線（docs/10 §2）。
VITE_API_BASE_URL=http://localhost:7077/api/v1

# reCAPTCHA v3 的 site key（docs/10 §5）。後台只有登入那一支用它。
#
# ⚠️ site key 是**公開值**，本來就會出現在 HTML 裡 —— 要保密的是 secret key，
#    那個只在 Function App 的 app settings（BotCheck__SecretKey）。
# ⚠️ 留空＝這個環境不啟用驗證。**兩邊要一起設**：前端留空但後端設了 secret key，
#    登入會因為「沒有帶 token」被擋下，而且錯誤訊息指不到這個原因。
VITE_RECAPTCHA_SITE_KEY=
